// Constants
export const GRID_MAX_WIDTH = 1500;
export const UNIT = GRID_MAX_WIDTH / 12; // = 100px / unit

// Utilities
// export function getUnitWidth(factor: number): string | undefined {
//      const value = factor > 0 && factor <= 12 ? factor * UNIT : undefined;
//      return value ? `${value}px` : undefined;
// }